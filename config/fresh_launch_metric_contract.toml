schema_version = "phase90.fresh_launch_metric_contract.v2"
missing_value_policy = "unavailable_not_zero"
stream_required_missing_policy = "fail_canary_and_stop_tracking"
threshold_tuning_allowed = false

[[metric]]
metric_id = "launch_create_signature"
metric_name = "launch create signature"
metric_family = "Pump create / launch metadata"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "launch_create_slot"
metric_name = "launch create slot"
metric_family = "Pump create / launch metadata"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "launch_create_timestamp"
metric_name = "launch create timestamp"
metric_family = "Pump create / launch metadata"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "mint"
metric_name = "mint"
metric_family = "Pump create / launch metadata"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "creator_dev_wallet"
metric_name = "creator dev wallet"
metric_family = "dev holdings"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "bonding_curve_account"
metric_name = "bonding curve account"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "associated_bonding_curve_account_observed"
metric_name = "associated bonding curve account observed"
metric_family = "bonding curve reserves"
bucket = "stream_optional"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "associated_bonding_curve_account_derived"
metric_name = "associated bonding curve account derived"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_decimals"
metric_name = "token decimals"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "bonding_curve_update|holder_balance_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_total_supply_stream_observed"
metric_name = "token total supply stream observed"
metric_family = "market cap"
bucket = "stream_optional"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_total_supply_bonding_curve_observed"
metric_name = "token total supply bonding curve observed"
metric_family = "market cap"
bucket = "stream_optional"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_total_supply_protocol_constant"
metric_name = "token total supply protocol constant"
metric_family = "market cap"
bucket = "research_derived"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_total_supply_rpc_verified"
metric_name = "token total supply rpc verified"
metric_family = "market cap"
bucket = "enrichment_required_rpc"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_total_supply_selected"
metric_name = "token total supply selected"
metric_family = "market cap"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "virtual_sol_reserves"
metric_name = "virtual sol reserves"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "virtual_token_reserves"
metric_name = "virtual token reserves"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "real_sol_reserves"
metric_name = "real sol reserves"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "real_token_reserves"
metric_name = "real token reserves"
metric_family = "bonding curve reserves"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "curve_complete_flag"
metric_name = "curve complete flag"
metric_family = "curve progress"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "curve_progress_pct"
metric_name = "curve progress pct"
metric_family = "curve progress"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "bonding_curve_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "buy_sell_direction"
metric_name = "buy sell direction"
metric_family = "buy/sell flow"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_amount_raw_or_ui"
metric_name = "token amount raw or ui"
metric_family = "buy/sell flow"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "quote_amount_lamports_or_sol"
metric_name = "quote amount lamports or sol"
metric_family = "buy/sell flow"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "signer_or_owner_wallet"
metric_name = "signer or owner wallet"
metric_family = "unique buyers / sellers"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|holder_balance_update|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "token_account"
metric_name = "token account"
metric_family = "holder count"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "holder_balance_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "pre_post_token_balance"
metric_name = "pre post token balance"
metric_family = "holder count"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "holder_balance_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "pre_sol_balance_stream_observed"
metric_name = "pre sol balance stream observed"
metric_family = "realized buy/sell trade price"
bucket = "stream_optional"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "post_sol_balance_stream_observed"
metric_name = "post sol balance stream observed"
metric_family = "realized buy/sell trade price"
bucket = "stream_optional"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "quote_amount_lamports_from_delta"
metric_name = "quote amount lamports from delta"
metric_family = "realized buy/sell trade price"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "transaction_signature"
metric_name = "transaction signature"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "instruction_type"
metric_name = "instruction type"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|pump_buy|pump_sell|bonding_curve_update|holder_balance_update"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "instruction_shape_hash"
metric_name = "instruction shape hash"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "account_list_hash"
metric_name = "account list hash"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "compute_unit_limit"
metric_name = "compute unit limit"
metric_family = "fee / priority fee / compute budget"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "compute_unit_price"
metric_name = "compute unit price"
metric_family = "fee / priority fee / compute budget"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "priority_fee"
metric_name = "priority fee"
metric_family = "fee / priority fee / compute budget"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "tx_fee"
metric_name = "tx fee"
metric_family = "fee / priority fee / compute budget"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "failed_transaction_flag"
metric_name = "failed transaction flag"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "pump_buy|pump_sell|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "error_code_if_failed"
metric_name = "error code if failed"
metric_family = "transaction fingerprints"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "source_sequence_slot_timestamp_confidence"
metric_name = "source sequence slot timestamp confidence"
metric_family = "run identity and timing"
bucket = "stream_required"
required_layer = "edge_collector"
source_artifact = "normalized_events"
required_event_types = "token_created|pump_buy|pump_sell|bonding_curve_update|holder_balance_update|observed_transaction"
required_accounts = "fresh mint and related curve/token accounts"
unit = "mixed"
confidence_logic = "observed from normalized event source field; unavailable is never coerced to zero"
unavailable_reason_if_missing = "classified by launch-metric-canary source-field coverage"
implementation_status = "implemented_or_provider_unavailable"
safe_for_diagnostics = true
safe_for_backtest = true
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "funding_graph"
metric_name = "funding graph"
metric_family = "funding graph"
bucket = "enrichment_required_rpc"
required_layer = "enrichment_required_rpc"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "blocked_by_rpc_budget_until credits/API budget available"
implementation_status = "enrichment_required_rpc"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "common_funder"
metric_name = "common funder"
metric_family = "common funder"
bucket = "enrichment_required_rpc"
required_layer = "enrichment_required_rpc"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "blocked_by_rpc_budget_until credits/API budget available"
implementation_status = "enrichment_required_rpc"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "wallet_age_history"
metric_name = "wallet age history"
metric_family = "wallet age / wallet history"
bucket = "enrichment_required_rpc"
required_layer = "enrichment_required_rpc"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "blocked_by_rpc_budget_until credits/API budget available"
implementation_status = "enrichment_required_rpc"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "metadata_social_fetch"
metric_name = "metadata social fetch"
metric_family = "metadata/socials"
bucket = "enrichment_required_http"
required_layer = "enrichment_required_http"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "HTTP metadata fetch only; no RPC in canary no-rpc mode"
implementation_status = "enrichment_required_http"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "raw_shred_metrics"
metric_name = "raw shred metrics"
metric_family = "raw-shred metrics"
bucket = "raw_shred_required"
required_layer = "raw_shred_required"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "requires raw shred feed/provider"
implementation_status = "raw_shred_required"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "deshred_metrics"
metric_name = "deshred metrics"
metric_family = "deshred metrics"
bucket = "deshred_required"
required_layer = "deshred_required"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "requires deshred provider/feed"
implementation_status = "deshred_required"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

[[metric]]
metric_id = "post_migration_pumpswap"
metric_name = "post migration pumpswap"
metric_family = "post-migration / PumpSwap metrics"
bucket = "post_migration_required"
required_layer = "post_migration_required"
source_artifact = "enrichment/raw/deshred/post-migration artifact"
required_event_types = ""
required_accounts = ""
unit = "mixed"
confidence_logic = "explicit budget/provider gate"
unavailable_reason_if_missing = "requires PumpSwap/post-migration support or stream-captured pool events"
implementation_status = "post_migration_required"
safe_for_diagnostics = false
safe_for_backtest = false
safe_for_threshold_tuning = false
coverage_requirement_for_tuning = 1.0

